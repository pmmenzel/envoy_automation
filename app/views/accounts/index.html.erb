<h1>Accounts</h1>
<ol>
	<ol>
	<% @accounts.each do |a| %>
		<li>
		<%= link_to("#{a.Name}", a ) %>
		</li>
	<% end %>
	</ol>
</ol>

<%= link_to "Login", user_session_path, :method => "get" %>
<%= link_to "Register", new_user_registration_path, :method => "get" %>